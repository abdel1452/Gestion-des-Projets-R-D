{% extends 'base.html.twig' %}

{% block title %}Portail Technique{% endblock %}

{% block body %}
<h1 class="text-center my-5">Portail Technique</h1>

<div class="row g-4">

    {% for project in projects %}
        <div class="col-md-12">
            <div class="d-flex p-3 bg-white shadow-sm rounded align-items-center justify-content-between">

                <div class="d-flex align-items-center gap-3">
                    <img src="{{ asset('uploads/covers/' ~ project.image) }}" alt="image" width="180" class="rounded shadow-sm">
                    <div>
                        <h5 class="mb-1">{{ project.name }}</h5>
                        <p class="mb-0 text-muted">{{ project.description }}</p>
                    </div>
                </div>

                <div class="text-end d-flex flex-column gap-2">
                    <a href="{{ path('app_project_production', {id: project.id}) }}" class="btn btn-outline-primary d-flex align-items-center justify-content-between gap-2">
                        🚀 Version Production
                        <span class="badge bg-success">Active</span>
                    </a>

                    <a href="{{ path('app_project_preproduction', {id: project.id}) }}" class="btn btn-outline-warning d-flex align-items-center justify-content-between gap-2">
                        🛠 Version Préproduction
                        <span class="badge bg-danger">Test</span>
                    </a>
                </div>

            </div>
        </div>
    {% endfor %}

</div>
{% endblock %}
